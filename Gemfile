source 'https://rubygems.org'
group :jekyll_plugins do
    gem 'jekyll', '~> 4.2'
    gem 'jekyll-archives'
    # gem 'jekyll-diagrams'  # Commenting out since it's causing build issues
    gem 'jekyll-email-protect'
    gem 'jekyll-feed'
    gem 'jekyll-imagemagick'
    gem 'jekyll-minifier'
    gem 'jekyll-paginate-v2'
    gem 'jekyll-scholar'
    gem 'jekyll-sitemap'
    gem 'jekyll-link-attributes'
    gem 'jekyll-twitter-plugin'
    gem 'jemoji'
    # Commenting out mini_racer as it's causing installation issues
    # gem 'mini_racer'
    gem 'unicode_utils'
    gem 'webrick'
    gem 'date', '~> 3.3'  # Adding explicit date gem to resolve dependency issue
    gem 'observer', '~> 0.1.1'  # Adding explicit observer gem
end
group :other_plugins do
    gem 'httparty', '~> 0.21.0'
    gem 'feedjira', '~> 3.2.1'
end
